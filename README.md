# BOILERPLATE FOR NODE-EXPRESS APPLICATION (CLEAN ARCHITECTURE) WITH TYPESCRIPT

-   Edit docker-compose.yml
-   Set-up index.ts and server.ts files
-   Run docker-compose build
-   Run docker-compose up
